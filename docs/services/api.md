# torbook-api (@torbook/api) — LEGACY

> **Not active.** This package is a migration leftover. The public gateway is now [`@torbook/gateway`](../gateway.md). Only `packages/api/src/test-helpers/test-services.ts` is still referenced by the e2e stack.

## Historical role

Former public HTTP gateway. Replaced by `@torbook/gateway` + `@torbook/monolith`.

## See instead

- [`gateway.md`](gateway.md) — public API and proxy routes
- [`../../Dockerfile`](../../Dockerfile) — production Docker image (replaces `packages/api/Dockerfile`)

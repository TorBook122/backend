name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production

    steps:
      - uses: actions/checkout@v4

      - name: Deploy stub
        run: |
          echo "Deploy pipeline stub — configure ECR + ECS Fargate when infrastructure is ready."
          echo "Steps: build Docker image → push to ECR → update ECS service."

      # TODO: Uncomment when AWS infrastructure is provisioned
      # - name: Configure AWS credentials
      #   uses: aws-actions/configure-aws-credentials@v4
      #   with:
      #     aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
      #     aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      #     aws-region: ${{ vars.AWS_REGION }}
      #
      # - name: Login to Amazon ECR
      #   uses: aws-actions/amazon-ecr-login@v2
      #
      # - name: Build and push Docker image
      #   run: |
      #     docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$GITHUB_SHA .
      #     docker push $ECR_REGISTRY/$ECR_REPOSITORY:$GITHUB_SHA
      #
      # - name: Deploy to ECS
      #   run: |
      #     aws ecs update-service --cluster torbook --service torbook-api --force-new-deployment
